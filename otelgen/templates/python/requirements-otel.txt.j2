# OpenTelemetry dependencies for {{ service_name }} ({{ framework.label }}).
# Append these to your existing requirements.txt, or install them directly:
#   pip install -r requirements-otel.txt
#
# The API and SDK packages share a version line (1.x); the instrumentation
# packages are on their own pre-1.0 line (0.5xbN) and must match the SDK
# release they were built against, so pin them together when you upgrade.
#
# Generated by {{ generator }}.
# Background reading: {{ article }}

opentelemetry-api>=1.30.0
opentelemetry-sdk>=1.30.0
{% if spec.protocol == 'grpc' %}
opentelemetry-exporter-otlp-proto-grpc>=1.30.0
{% elif spec.protocol == 'http' %}
opentelemetry-exporter-otlp-proto-http>=1.30.0
{% else %}
# Console exporter ships inside opentelemetry-sdk; no exporter package needed.
{% endif %}
{% if framework.key == 'fastapi' %}
opentelemetry-instrumentation-fastapi>=0.51b0
opentelemetry-instrumentation-asgi>=0.51b0
{% elif framework.key == 'django' %}
opentelemetry-instrumentation-django>=0.51b0
opentelemetry-instrumentation-wsgi>=0.51b0
{% elif framework.key == 'flask' %}
opentelemetry-instrumentation-flask>=0.51b0
opentelemetry-instrumentation-wsgi>=0.51b0
{% elif framework.key == 'celery' %}
opentelemetry-instrumentation-celery>=0.51b0
{% endif %}

# Outbound calls. Without one of these, spans stop at your service boundary and
# the trace looks like it ends there.
opentelemetry-instrumentation-requests>=0.51b0
# opentelemetry-instrumentation-httpx>=0.51b0
# opentelemetry-instrumentation-urllib3>=0.51b0

# Datastores, if you use them:
# opentelemetry-instrumentation-sqlalchemy>=0.51b0
# opentelemetry-instrumentation-psycopg2>=0.51b0
# opentelemetry-instrumentation-redis>=0.51b0
